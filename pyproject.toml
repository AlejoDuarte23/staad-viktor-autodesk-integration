[project]
name = "pullelementstaad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "comtypes>=1.4.13",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.17.0",
]
